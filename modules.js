const username = "codewitgabi";
const passkey = "12345";

function volume(l, b, h) {
  return (l * b * h);
}

module.exports = {
  username,
  passkey,
  volume
};

console.log(module.id);
console.log(module.filename);